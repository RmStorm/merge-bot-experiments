name: slow-green

on:
  pull_request:
  workflow_dispatch:

jobs:
  wait-and-pass:
    runs-on: ubuntu-latest
    steps:
      - name: Wait 180 seconds
        run: sleep 180

      - name: Pass
        run: echo "green"
